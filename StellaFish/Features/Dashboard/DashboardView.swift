import SwiftUI

struct DashboardView: View {
    var body: some View {
        NavigationStack {
            Text("Dashboard — Phase 2")
                .navigationTitle("StellaFish")
        }
    }
}
